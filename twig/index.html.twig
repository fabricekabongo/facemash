{% extends "layout.html.twig" %}
{% block contenu %}
            <div class="row-fluid">
                <div class="span6 offset3 center">
                    <p>
                        <strong>We are let in for our looks? No. Will we be judged on them? Yes.</strong>
                    </p>
                    <h3>Who's Hotter? Click to Choose.</h3>
                </div>
            </div>
            <div class="row-fluid">
                <div class="span3 offset3">
                     <a class="thumbnail voter" href="{{ path('facemash',{ 'choix':'gauche'}) }}">
                         <img src="{{ app.chemin_public }}images/{{ gauche }}" />
                    </a>
                </div>
                <div class="span1">
                    <p>OR</p>
                </div>
                <div class="span4">
                    <a class="thumbnail voter" href="{{ path('facemash',{ 'choix':'droite'}) }}">
                        <img src="{{ app.chemin_public }}images/{{ droite }}" />
                    </a>
                </div>
            </div>
{% endblock %}
{% block link %}
<a href="{{ path('upload') }}"><h4>Upload more images</h4></a>
{% endblock %}